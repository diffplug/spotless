#!/usr/bin/env bash

function foo() {
  if [ -x $file ]; then
    myArray=(item1 item2 item3)
  elif [ $file1 -nt $file2 ]; then
    unset myArray
  else
    echo "Usage: $0 file ..."
  fi
}

for ((i = 0; i < 5; i++)); do
  read -p r
  print -n $r
  wait $!
done
