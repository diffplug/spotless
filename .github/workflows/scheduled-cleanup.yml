name: Scheduled Cleanup 🧹
on:
  schedule:
    - cron: '0 16 * * 3'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  sanity:
    name: Sanity 🦢
    runs-on: ubuntu-latest
    env:
      buildcacheuser: ${{ secrets.BUILDCACHE_USER }}
      buildcachepass: ${{ secrets.BUILDCACHE_PASS }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - name: Gradle Setup 🐘
        uses: gradle/actions/setup-gradle@v6
      - name: Error Prone 🚧️
        run: ./gradlew assemble -Derror-prone=true
      - name: OpenRewrite ☑️ # 4m 55s (2 min compile, 3 min rewrite)
        run: ./gradlew rewriteDryRun
      # Catches upstream drift between the embedded lockfiles and Eclipse's P2 metadata. It lives
      # here rather than in CI because it queries the Eclipse update sites -- per-PR gaps and strays
      # are already caught offline by EclipseJdtFormatterStepTest.
      - name: Eclipse JDT Lockfiles 🔒 # ~1 min
        run: ./gradlew :lib-extra:verifyEclipseJdtLockfiles
